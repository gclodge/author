namespace Author.Domain.Constants;

/// <summary>
/// Contains the names of custom claims used in the application.
/// </summary>
public abstract class ClaimNames
{
    public const string Policy = nameof(Policy);
    public const string Role = nameof(Role);

    //< Add any other custom claim names here as needed
}

